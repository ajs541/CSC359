import React, { useState } from "react";
import styled from "styled-components";

// used a tutorial from
// https://react.school/ui/button

const theme = {
    blue: {
        default: "#3f51b5",
        hover: "#283593"
    },
    pink: {
        default: "#e91e63",
        hover: "#ad1457"
    },
}